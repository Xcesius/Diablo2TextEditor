# Core dependencies for Diablo II Text Editor
# Note: tkinter, pathlib, json are usually included with Python

pytest>=8.4.1
pytest-cov>=6.2.1
chardet>=5.2.0
black>=25.1.0
flake8>=7.3.0
PyQt6>=6.9.1

# For PDF processing, install: pip install -r requirements_pdf.txt 
